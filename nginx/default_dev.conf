upstream api {
    server api:8000;
}

upstream pgadmin {
    server pgadmin:80;
}

server {
    listen 8000;

    location / {
        proxy_pass http://api;
    }
}

server {
    listen 5050;

    location / {
        proxy_pass http://pgadmin;
    }
}